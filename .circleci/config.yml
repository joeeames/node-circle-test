version: 2.1
jobs:
  build-android:
    docker:
      - image: circleci/android:api-25-alpha
      build:
        docker:
          - image: circleci/node:4.8.2
        steps:
          - checkout
          - run: echo "hello world"
